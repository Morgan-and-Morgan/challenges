// Note: In a real production scenario, we would use the ExtractTextPlugin to write the compiled CSS to a separate file
require('../styles/index.scss');


class Autocomplete {
    /**
     * the bind method takes a DOM Element and does any work necessary to setup the autocomplete widget
     * TODO: Implement Me!
     * @param domEl {DOMElement}
     * @return bool
     */
    bind(domEl) {
        return true;
    }
}


export default Autocomplete;
